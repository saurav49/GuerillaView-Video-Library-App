const backEndURL = `https://guerrillaViewBackend.saurav49.repl.co`;
const getALLvideosURL = `${backEndURL}/videos`;
const playlistURL = `${backEndURL}/playlist`;
const videoNoteURL = `${backEndURL}/videos/notes`;
const historyURL = `${backEndURL}/history`;
const watchLaterURL = `${backEndURL}/watchLater`;
const likedURL = `${backEndURL}/liked`;

export {
  backEndURL,
  getALLvideosURL,
  playlistURL,
  videoNoteURL,
  historyURL,
  watchLaterURL,
  likedURL,
};
